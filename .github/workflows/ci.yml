name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# ВАЖНО (мульти-репо): go.mod содержит replace на ../linkpulse-contracts,
# поэтому CI дополнительно чекаутит contracts в соседнюю папку. После публикации
# contracts как Go-модуля replace и второй checkout удаляются.
jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          path: linkpulse-link
      - uses: actions/checkout@v5
        with:
          repository: 1RAFTIK1/linkpulse-contracts
          path: linkpulse-contracts
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          cache: true
          cache-dependency-path: linkpulse-link/go.sum
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.11.4
          working-directory: linkpulse-link

  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          path: linkpulse-link
      - uses: actions/checkout@v5
        with:
          repository: 1RAFTIK1/linkpulse-contracts
          path: linkpulse-contracts
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          cache: true
          cache-dependency-path: linkpulse-link/go.sum
      - name: go test (race + coverage)
        working-directory: linkpulse-link
        run: go test -race -coverprofile=coverage.out ./...
      - name: coverage summary
        working-directory: linkpulse-link
        run: go tool cover -func=coverage.out | tail -1

  security:
    name: security scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          path: linkpulse-link
      - uses: actions/checkout@v5
        with:
          repository: 1RAFTIK1/linkpulse-contracts
          path: linkpulse-contracts
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          cache: true
          cache-dependency-path: linkpulse-link/go.sum
      # Известные уязвимости в зависимостях (по вызываемым путям, не просто по go.sum).
      - name: govulncheck
        working-directory: linkpulse-link
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...
      # Статический анализ безопасности Go-кода.
      - name: gosec
        working-directory: linkpulse-link
        run: |
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          gosec ./...

  docker:
    name: docker build & scan
    runs-on: ubuntu-latest
    needs: [lint, test]
    permissions:
      contents: read
      packages: write # push в GHCR
    steps:
      - uses: actions/checkout@v5
        with:
          path: linkpulse-link
      - uses: actions/checkout@v5
        with:
          repository: 1RAFTIK1/linkpulse-contracts
          path: linkpulse-contracts

      # Контекст — родительская папка (см. комментарий в Dockerfile).
      - name: Build image
        run: docker build -f linkpulse-link/Dockerfile -t ghcr.io/${{ github.repository }}:${{ github.sha }} .

      # Скан образа на уязвимости; HIGH/CRITICAL валят сборку.
      - name: Trivy scan
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: ghcr.io/${{ github.repository }}:${{ github.sha }}
          format: table
          exit-code: "1"
          severity: HIGH,CRITICAL

      # CD-часть: на main публикуем образ в GitHub Container Registry.
      - name: Login to GHCR
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image
        if: github.ref == 'refs/heads/main'
        run: |
          docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:latest
          docker push ghcr.io/${{ github.repository }}:${{ github.sha }}
          docker push ghcr.io/${{ github.repository }}:latest
